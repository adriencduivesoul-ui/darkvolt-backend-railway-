# DarkVolt Backend

Backend API + Socket.IO for DarkVolt streaming platform.

## Deploy on Railway

- Node.js 22
- Environment variables:
  - `NODE_ENV=production`
  - `ALLOWED_ORIGIN=https://your-domain.com`

## Build & Start

```bash
pnpm install
pnpm run build
pnpm start
```
